### **問題の概要**

これは、**ハッシュDOS**（Hash Collision Denial of Service）の脆弱性を利用したCapture The Flag（CTF）の問題です。この問題は、サーバーサイドでハッシュテーブルが使われている入力フォームに対して、意図的にハッシュ衝突を引き起こすことで、サーバーの応答を遅延させたり、クラッシュさせたりすることを目指します。

-----

### **問題設定**

あなたはウェブサーバーを攻撃するハッカーとして、以下のウェブサイトにアクセスしました。

ウェブサイトのURL: `http://challenge.example.com`

-----

### **CTFシナリオ**

ウェブサイトには、ユーザー登録用のフォームがあります。このフォームは、ユーザー名とパスワードを送信するPOSTリクエストを受け付けます。

**フォームのHTML:**

```html
<form action="/register" method="post">
  <label for="username">Username:</label><br>
  <input type="text" id="username" name="username"><br>
  <label for="password">Password:</label><br>
  <input type="password" id="password" name="password"><br><br>
  <input type="submit" value="Register">
</form>
```

**サーバーサイドの挙動:**

このアプリケーションはゲートウェイとPHPサーバーでできています。ゲートウェイはサーバーがダウンしていることを検知するとフラグを表示します。

このサーバーは、**PHP**で書かれており、フォームから送信されたPOSTデータを処理する際に、\*\*連想配列（ハッシュマップ）\*\*を使用しています。具体的には、$\_POSTスーパーグローバル変数にアクセスする際に、内部でハッシュテーブルが構築されます。

サーバーは、ユーザーが送信したPOSTデータを受け取った後、`$_POST`変数にアクセスする処理を以下のように実行しています。

```php
<?php
// register.php
// ...

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    // この行で$_POST変数にアクセスし、内部でハッシュテーブルが構築・操作される
    $username = $_POST['username'];
    $password = $_POST['password'];
    // ... その他の処理（時間のかかる処理はない）
}
// ...
?>
```

-----

### **ゴール**

**あなたの目標は、POSTリクエストを送信してサーバーの応答を意図的に遅延させ、最終的にサーバーの処理をタイムアウトさせることです。**

この脆弱性を利用するためには、**同じハッシュ値を持つ大量のキー**をPOSTデータに含めて送信する必要があります。これにより、PHPの連想配列が内部でハッシュ衝突を繰り返し、各キーの検索や挿入に膨大な時間がかかるようになり、サーバーがダウンします。

-----

### **ヒント**

1.  **POSTリクエストの構造:** POSTリクエストのボディは `key1=value1&key2=value2&...` という形式になっています。
2.  **ハッシュ衝突:** ハッシュDOS攻撃は、ハッシュ関数の特定の特性（この場合はPHPのハッシュ関数）を悪用します。
3.  **攻撃のペイロード:** 攻撃に必要なデータは、キー名です。例えば、`key[]=value`のように配列として送信すると、同じ名前のキーが大量に作られます。
4.  **PHPのハッシュ関数:** 攻撃に利用するハッシュ値の計算方法を特定するために、PHPの内部ハッシュ関数について調べてみましょう。
5.  **ツールの利用:** `curl`や`Python`の`requests`ライブラリなど、POSTリクエストをカスタマイズして送信できるツールが役立ちます。

**フラグ:**

サーバーがタイムアウトし、フラグが記載されたエラーメッセージがレスポンスとして返ってきます。

例: `CTF{HASH_COLLISION_IS_A_SERVER_KILLER}`

-----

### **解説（運営者向け）**

  * **脆弱性の詳細:** この問題は、PHPのバージョン5.3.9以降に修正された脆弱性（CVE-2011-4876）に基づいています。しかし、この問題では意図的に脆弱な設定を再現しています。
  * **ハッシュアルゴリズム:** PHPの古いバージョンでは、文字列をハッシュ化する際にDJBハッシュ関数が使われていました。このアルゴリズムは、`'s' + '\x00'` と `__` のように、特定のパターンを持つ文字列で簡単に衝突を引き起こせます。
  * **攻撃手法:**
    1.  POSTリクエストのボディに、同じハッシュ値を持つキーを大量に追加します。
    2.  例えば、`a=1&a=2&a=3...` のように単純なキーではなく、ハッシュ衝突を引き起こす特別なキー（例：`__`や`_`を組み合わせたキー）を数千から数万個作成します。
    3.  配列形式 (`key[]=value`) を使うと、同じキー名で複数の値を送信できるため、効率的にハッシュ衝突を起こせます。
  * **成功条件:**
      * 攻撃者は、`$_POST`にアクセスしただけで、サーバーが長時間フリーズするのを観測できます。
      * サーバーのタイムアウト設定（例: 30秒）を超えると、リクエストはエラーとなり、攻撃が成功したと判断できます。
      * 問題の難易度を調整するために、必要なキーの数を変更できます。
